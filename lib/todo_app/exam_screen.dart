import 'package:flutter/material.dart';

class ExamQuestion extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Text('Exam'),
      ),
      body: Center(child: Text('Final Exam')),
    );
  }
}
